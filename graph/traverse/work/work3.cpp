#include <cstdio>
#include <stack>
#include <vector>

using namespace std;

/**
 * 非递归实现(采用邻接表)图的DFS遍历
 */

const int MAX_SIZE = 1000, MAX_V = 100;

int n;
vector<int> Adj[MAX_SIZE];

int main() {
    return 0;
}